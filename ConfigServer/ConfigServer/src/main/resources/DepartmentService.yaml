server:
  port: 8701

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8700/eureka/
  instance:
    prefer-ip-address: true

management:
  tracing:
    sampling:
      probability: 1.0
